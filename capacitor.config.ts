import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter-dani',
  appName: 'ionic-angular-1',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
